{
  "name": "@api-docs/spec",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "type": "module",
  "devDependencies": {
    "@stoplight/spectral-cli": "^6.15.0",
    "@types/express": "^5.0.5",
    "@types/swagger-ui-express": "^4.1.8",
    "express": "^5.1.0",
    "swagger-cli": "^4.0.4",
    "swagger-ui-express": "^5.0.1",
    "tsx": "^4.20.6"
  },
  "scripts": {
    "lint": "spectral lint openapi.yaml",
    "bundle:yaml": "swagger-cli bundle openapi.yaml -o dist/openapi.bundle.yaml -t yaml",
    "bundle:json": "swagger-cli bundle openapi.yaml -o dist/openapi.bundle.json -t json",
    "bundle:index": "tsc --project tsconfig.json",
    "bundle": "npm run bundle:yaml && npm run bundle:json && npm run bundle:index",
    "check": "npm run lint && npm run bundle",
    "ui": "node src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
